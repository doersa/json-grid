{
  "name": "json-grid",
  "version": "1.0.0",
  "private": true,
  "description": "JSON → 表格分栏预览工具（Web 页面 + Apifox 可视化双模式）",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "node tests/equivalence.mjs && node tests/page-smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1"
  }
}
